# Extra dependencies needed ONLY to retrain from scratch (python -m src.train).
# The deployed app (requirements.txt) does not need TensorFlow: production
# inference runs the RNNs as TFLite via the lightweight ai-edge-litert
# interpreter, not the full TensorFlow runtime (see src/predict.py).
-r requirements.txt
tensorflow==2.15.1
tf-keras==2.15.1
# For the fine-tuned-transformer experiment (experiments/transformer_finetune.py)
# and the optional 4th ensemble member. torch is already in requirements.txt;
# transformers is training-only until the promotion gate adopts the model, at
# which point it also belongs in requirements.txt for runtime inference.
transformers==4.46.3
